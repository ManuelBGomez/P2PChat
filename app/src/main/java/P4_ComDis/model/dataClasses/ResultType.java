package P4_ComDis.model.dataClasses;

/**
 * Enumerado que representa el tipo de resultado que se puede devolver
 */
public enum ResultType {
    OK,
    UNAUTHORIZED,
    ALREADY_CONNECTED,
    DATABASE_ERROR,
    ALREADY_FRIENDS, NOT_VALID
}
